// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     // output: 'export',
// };

// export default nextConfig;

export default {
    images: {
      unoptimized: true,
    },
    trailingSlash: true,
    output: 'npx serve@latest out',
};
